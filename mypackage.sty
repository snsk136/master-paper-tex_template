%======================================================= for 画像取り込みの為
\usepackage[dvipdfmx]{graphicx}
\usepackage{float}
\usepackage{wrapfig}            %テキストを図に回り込ませる
\usepackage{placeins}           %for FloatBarrier

%=======================================================
\usepackage[utf8]{inputenc}
\usepackage{amsmath}            %数式のやつ
\usepackage{amsmath,amssymb}
\usepackage{siunitx}            %SI単位のやつ
\usepackage[version=4]{mhchem}  %化学のやつ
\usepackage{indentfirst}        %インデントを入れる
\usepackage[numbers]{natbib}    %参考文献を参\cite{}するやつ
\usepackage{comment}
%サブキャプションを入れて図を並べて表示
\usepackage[hang,small,labelsep=space]{caption} %hangはcaptionのインデント、smallは文字サイズ、spaceは図〇とタイトルの間を指定
\usepackage[subrefformat=parens]{subcaption}
\captionsetup{compatibility=false}

\renewcommand{\bibname}{参考文献}

%=======================================================余白調整
\usepackage[twoside,top=35truemm,bottom=30truemm,inner=35truemm,outer=30truemm]{geometry}
\setlength{\footskip}{5pt}

%=======================================================ヘッダーフッダーの見栄え
\usepackage{fancyhdr}

\pagestyle{fancy}  % C: 中央, R: 右, L: 左， E: 偶数ページ, O: 奇数ページ
\fancyhf{}         %ヘッダとフッダーの初期化
\fancyhead[LO]{\rightmark}
\fancyhead[RO]{\thepage}
\fancyhead[RE]{\leftmark}
\fancyhead[LE]{\thepage}

\renewcommand{\chaptermark}[1]{\markboth{第\ \thechapter\ 章~#1}{}}
\renewcommand{\sectionmark}[1]{\markright{\thesection #1}{}}


%=======================================================ショートカット
\newcommand{\hakushi}{
    \newpage
    \thispagestyle{empty}
    ~
    \newpage
    }

%%===============図表
\renewcommand{\figurename}{図}
\renewcommand{\tablename}{表}

% 参照を楽にするコマンド
\def \fref 		  #1{\figurename\ref{#1}}
\def \tref 		  #1{\tablename\ref{#1}}
\def \equref 	  #1{式(\ref{#1})}
\def \chapref 	  #1{第\ref{#1}章}
\def \appendixref #1{付録\ref{#1}}
\def \secref	  #1{\ref{#1}節}
\def \subsecref	  #1{\ref{#1}小節}


%%===============化学式



%================Tcなど
\newcommand{\Tc}        {T_{\mathrm{c}}}
\newcommand{\Tczero}    {T_{\mathrm{c}}^{\mathrm{zero}}}
\newcommand{\Tconset}   {T_{\mathrm{c}}^{\mathrm{onset}}}
\newcommand{\Tcdia}     {T_{\mathrm{c}}^{\mathrm{dia}}}
\newcommand{\Tcx}       {T_{\mathrm{c}}^{\mathrm{\chi}}}
\newcommand{\Hc}      	{H_{\mathrm{c}}}
\newcommand{\Hco}       {H_{\mathrm{c1}}}
\newcommand{\Hct}       {H_{\mathrm{c2}}}
\newcommand{\Pc}        {P_{\mathrm{c}}}


% %=======================================================for hyperref
\usepackage[dvipdfmx]{hyperref}
\usepackage{pxjahyper}
\hypersetup{%
% hidelinks,            %==============印刷のためリンクの色を消す
colorlinks=true,        %==============色を付けたいときはこっち
citecolor=blue,
linkcolor=blue,
urlcolor=blue,
}


%--------------------------以下提出の際にはコメントアウト
% \setcounter{tocdepth}{3}                 %一時的に目次を深く表示
% \renewcommand{\baselinestretch}{1.3}     %行間を開ける(既定のx倍)

